# Modules

Product behavior is organized by business capability. Each use-case folder owns the behavior needed for that capability. Framework infrastructure belongs in `platform`; reusable presentation and utilities belong in `shared`.
